import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#1A1A1A",
  },

  tasksContainer: {
    flex: 1,
    width: "100%",
    padding: 24,
    paddingTop: 50,
  },
});
